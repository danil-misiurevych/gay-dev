{
  "name": "slice-lab",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Browser game: a space bar, slicing ingredients. Playgama / YouTube Playables.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test tests/*.test.js",
    "size": "node scripts/check-size.mjs"
  },
  "dependencies": {
    "three": "^0.160.1"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
